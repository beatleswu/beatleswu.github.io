[pytest]
testpaths = tests
norecursedirs =
    .git
    .hg
    .svn
    .tox
    .nox
    .venv
    venv*
    build
    dist
    *.egg
    *worktree*
markers =
    expected_red_on_b3cb: contract intentionally expected to fail before Lane A V1A integration
