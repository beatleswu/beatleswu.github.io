[pytest]
testpaths = tests
python_files = test_*.py
addopts = -ra
markers =
    backend: tests that require the Flask backend app module
    integration: tests requiring curated real files or service-level integration
    pending: intentionally pending coverage with a documented blocker
    e2e: Playwright browser tests; not collected by pytest

